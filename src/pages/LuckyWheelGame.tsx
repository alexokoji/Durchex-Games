import {useState} from 'react';import {Box,Button,Typography,Card} from '@mui/material';import {useWallet} from '../contexts/WalletContext';import {useAuth} from '../contexts/AuthContext';import {useToasts} from '../contexts/ToastContext';import {neonGreen,neonGold,darkCard,darkBorder} from '../theme';import {GameEngine} from '../games/shared/GameEngine';import {LuckyWheelGame} from '../games/luckyWheel/LuckyWheelGame';export default function LuckyWheelGamePage(){const wallet=useWallet();const {user}=useAuth();const toasts=useToasts();const [stake,setStake]=useState(100);const [gameState,setGameState]=useState({gameOver:false,won:false});const [loading,setLoading]=useState(false);const [_engine]=useState(()=>new GameEngine());const [game]=useState(()=>new LuckyWheelGame());const playGame=async()=>{if(!user||stake>wallet.balance){toasts.error('Insufficient balance','Check your wallet');return;}setLoading(true);try{const result=await game.play({gameId:'luckywheel',userId:user.id,sessionId:`lw_${Date.now()}`,stake,currency:wallet.currency,autoPlayCount:0,mode:'single'});setGameState({gameOver:true,won:result.won});const bet=await wallet.placeBet({gameId:'luckywheel',gameName:'Lucky Wheel Plus',stake});if(bet)await wallet.settleBet(bet.id,{won:result.won,payout:result.payout,multiplier:result.multiplier});toasts.success(result.won?'Won!':'Lost',result.won?`${result.multiplier.toFixed(2)}x!`:'No match');}catch(e:any){toasts.error('Error',e.message);}finally{setLoading(false);}};return(<Box sx={{p:3,maxWidth:500,mx:'auto'}}><Typography sx={{fontSize:'2rem',fontWeight:900,mb:3,textAlign:'center'}}>🎡 Lucky Wheel Plus</Typography><Card sx={{background:darkCard,border:`1px solid ${darkBorder}`,borderRadius:2,p:3}}><Box sx={{mb:3}}><Typography sx={{fontSize:'0.85rem',fontWeight:700,mb:1}}>Stake</Typography><input type="number"value={stake}onChange={e=>setStake(Math.max(1,parseInt(e.target.value)||1))}disabled={loading||gameState.gameOver}style={{width:'100%',padding:'8px',borderRadius:4,border:`1px solid ${darkBorder}`,background:'#1a1a2e',color:'#fff'}}/></Box><Button fullWidth variant="contained"disabled={loading||gameState.gameOver}onClick={playGame}sx={{background:neonGreen,color:'#000',fontWeight:900,py:1.5,mb:2}}>{loading?'Spinning...':'Spin Wheel'}</Button>{gameState.gameOver&&<Box sx={{p:2,background:'#1a1a2e',borderRadius:1,textAlign:'center',mb:2}}><Typography sx={{fontSize:'1.1rem',fontWeight:800,color:gameState.won?neonGreen:'#ff6b7a'}}>{gameState.won?'✓ Win!':'✗ Lose'}</Typography></Box>}{gameState.gameOver&&<Button fullWidth variant="outlined"onClick={()=>setGameState({gameOver:false,won:false})}sx={{borderColor:neonGold,color:neonGold}}>Play Again</Button>}</Card><Box sx={{mt:3,textAlign:'center'}}><Typography sx={{fontSize:'0.9rem',color:'text.secondary',mb:1}}>Balance</Typography><Typography sx={{fontSize:'1.3rem',fontWeight:900,color:neonGreen}}>{wallet.balance.toFixed(2)} {wallet.currency}</Typography></Box></Box>);}